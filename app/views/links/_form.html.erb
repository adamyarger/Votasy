<%= form_for :link, url: links_path do |f| %>
    <%= f.text_field :content, placeholder: "Submit a link", class: 'full-width', id: "url" %>
    <br>
    <%= f.submit "Submit Link", class: "full-width" %>
<% end %>