<%= form_for :link, url: links_path do |f| %>
    <%= f.text_field :content, placeholder: "URL from, Soundcloud, Youtube or Vimeo", class: 'full-width', id: "url" %>
    <br>
    <%= f.submit "Submit Post", class: "full-width" %>
<% end %>